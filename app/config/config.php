<?php
define($DS, DIRECTORY_SEPARATOR);
define($APP, "app" . $DS);

define($CONFIG, array(
    "CONTROLLER" => $APP . "controllers" . $DS,
    "VIEWS" => $APP . "views" . $DS,
    "MODEL" => $APP . "model" . $DS,
    "EXT_PHP" => '.php'
));
